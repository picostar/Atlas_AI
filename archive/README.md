# Archive

Use this folder for superseded artifacts, one-off outputs, and temporary files that should not stay mixed with active work.

Recommended practice:
- keep `archive/` out of the active workflow
- gitignore it if the repository should not track archived artifacts
- move files here instead of leaving clutter in the repo root